СДЕЛАНО 1.radiobuttons: поиск по фамилии, имени, отчеству на стр. "Посмотреть запись в тел.книге".

СДЕЛАНО 2. разобраться почему получаются иероглифы при внесении данных на русск. языке СДЕЛАНО

СДЕЛАНО 3. добавить функцию добавления телефонов к существующему имени на странице 
"Посмотреть запись в тел книге".
	3.a. заблокировать возможность многократного внесения одних и тех же данных при повторном нажатии кнопки сохранить данные.
	3.б. при пустом input данные на сервер не отправлять (by disabling the button).

СДЕЛАНО 4. добавить функцию удаления как отдельных телефонов так и целиком персоны вместе
со всеми телефонами на странице "Посмотреть запись в тел. книге".

5. во время ожидания загрузки имен и телефонов появляется значок-анимация
при окончании загрузки он исчезает.



Usecase:
при отсутствии данных в div.two, то есть когда нет параграфов в div.two не активны
кнопки: добавить телефон к имени, удалить телефон, удалить имя со всеми телефонами.
Пока не щелкнули на конкретном телефоне кнопка удалить телефон не активирована.
При нажатии на эту кнопку появляется диалоговое окно да-нет "Хотите ли вы действительно
удалить этот номер телефона"



		$("div.two p").click(function(){ //to do here: choose all but first <P>
			var phnId =$(this).attr("id");//get phone's id
			alert(phnId);
			var phoneIdObj = {phoneId:phnId};
			$("button.add_delete_phone").removeAttr("disabled");
			$("button.add_delete_phone").click(function(){
				$.get("http://localhost:8080/phoneBook/deletephone", phoneIdObj, function(data){
					alert(data);
				});//end of get .../deletephone	
			}); //end of $("button.add_delete_phone").click
			
		});//end of "div.two p" click() function


